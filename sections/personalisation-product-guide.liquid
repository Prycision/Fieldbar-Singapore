{% schema %}
{
  "name": "Personalisation Guide",
  "tag": "section",
  "class": "personalisation-guide",
  "settings": [
    {
      "type": "image_picker",
      "id": "left_image",
      "label": "Left Column Background Image"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Sustainable Craftsmanship"
    },
    {
      "type": "richtext",
      "id": "body_text",
      "label": "Body Text",
      "default": "<p>We want to revive the belief system that our grandparents and the generations before them had, which is to buy something of <strong>quality</strong> and <strong>longevity</strong> and then <strong>repair</strong> and <strong>care</strong> for that item.</p><p>This <strong>return-to-quality</strong> is, we believe, the <strong>foundation of sustainable</strong> design. And it's why all parts of our products are <strong>repairable</strong> and <strong>replaceable</strong>. Because you <strong>make something differently</strong> when you know it has to <strong>last forever.</strong></p>"
    },
    {
      "type": "text",
      "id": "link_text",
      "label": "Button Text",
      "default": "Discover Our Craft"
    },
    {
      "type": "url",
      "id": "link_url",
      "label": "Button Link"
    },
    {
      "type": "image_picker",
      "id": "badge_image",
      "label": "Badge Image"
    }
  ],
  "presets": [
    {
      "name": "Personalisation Guide"
    }
  ]
}
{% endschema %}

<div class="sustainable-craft-section">
  <div class="craft-left" style="background-image:url('{{ section.settings.left_image | image_url }}')">
    <!-- cover background image -->
  </div>

  <div class="craft-righty">
    <div class="craft-overlay">
      <h2 class="craft-title">{{ section.settings.heading }}</h2>
      <div class="craft-text rte">{{ section.settings.body_text }}</div>
      {% if section.settings.link_url %}
        <a href="{{ section.settings.link_url }}" class="cap-text">
          {{ section.settings.link_text }}
        </a>
      {% endif %}
    </div>
    {% if section.settings.badge_image %}
      <div class="craft-badge">
        <img src="{{ section.settings.badge_image | image_url }}" alt="">
      </div>
    {% endif %}
  </div>
</div>

<style>
  .sustainable-craft-section {
    display: flex;
    flex-direction: row;
    width: 100%;
    height: 80vh;
    gap: 2em;
    padding: 2em 2em;
    box-sizing: border-box;
  }
  .craft-left {
    width: 40%;
    background-size: cover;
    background-position: center center;
  }
  .craft-righty {
    width: 60%;
    padding: 0 2em;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    color: #002f4d;
  }
  .craft-title {
    font-size: 2em;
    font-family: 'SurveyorText Book Light Pro', serif;
    margin-bottom: 0.5em;
  }
  .craft-overlay {
        align-self: flex-start !important;
  }
  .craft-text {
    margin-bottom: 1em;
  }
  .cap-text {
    display: inline-block;
    align-self: flex-start !important;
    margin-top: 2em;
    right:0;
    position: relative;
    color: #002d53;
    text-decoration: none;
    font-weight: 600;
    transition: color 0.3s ease;
  }
  .cap-text::after {
    content: "";
    position: absolute;
    left: 0; bottom: -2px;
    width: 100%; height: 2px;
    background-color: #FF8A3D;
    transition: transform 0.3s ease;
    transform-origin: right;
  }
  .cap-text:hover {
    color: #FF8A3D;
  }
  .cap-text:hover::after {
    transform: scaleX(0);
  }
  .craft-badge {
    text-align: right;
  }
  .craft-badge img {
    width: 50%;
    height: auto;
  }

  /* Mobile / tablet: vertical stack */
  @media (max-width: 768px) {
    .sustainable-craft-section {
      flex-direction: column;
      height: auto;
      gap: 1em;
      padding: 1em;
    }
    .craft-left {
      width: 100%;
      height: 50vh;
    }

    .craft-righty {
      width: 100%;
      padding: 1.5em 0;
    }
    .craft-righty .craft-overlay {
          display: flex-start !important;
    }
    
    .sustainable-craft-section .craft-righty {
     height: 45vh!important;


      }
    
    .craft-text {
      columns: 1;
    }
    .cap-text {
      margin-top: 1.5em;
    }
    .craft-badge img {
      width: 70%;
      margin-top: 1em;
    } 
    .rte>*:last-child {
margin-bottom: 1em;
}
  }
</style>
